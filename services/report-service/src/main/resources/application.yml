spring:
  application:
    name: report-service
  profiles:
    active: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/datasheild_reports
    username: datasheild_admin
    password: datasheild_secure_123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8014
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    io.datasheild: DEBUG
  pattern:
    console: "%d{ISO8601} %-5level [%thread] %logger{36} - %msg%n"

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
